name: Kubernetes Manifest Validation

on:
  push:
    branches: [main]
    paths:
      - "k8s/**"
  pull_request:
    branches: [main]
    paths:
      - "k8s/**"

jobs:
  validate:
    name: Validate Kubernetes Manifests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Kubeconform
        uses: bmuschko/setup-kubeconform@v1

      - name: Validate Kubernetes manifests
        run: |
          kubeconform -summary -output json \
            -schema-location default \
            -schema-location 'https://raw.githubusercontent.com/datreeio/CRDs-catalog/main/{{.Group}}/{{.ResourceKind}}_{{.ResourceAPIVersion}}.json' \
            -skip SealedSecret,IngressRoute,IngressRouteTCP,Middleware,ServersTransport \
            -ignore-filename-pattern 'release\.yaml' \
            -ignore-filename-pattern 'release\.lock' \
            -ignore-filename-pattern 'values\.yaml' \
            -ignore-filename-pattern 'secret\.template\.yaml' \
            k8s/apps/ k8s/infra/

      - name: Validate Helm values (syntax check)
        run: |
          for file in $(find k8s/apps k8s/infra -name "values.yaml" -type f); do
            echo "Checking $file..."
            python3 -c "import yaml; yaml.safe_load(open('$file'))"
          done
